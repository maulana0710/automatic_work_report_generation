services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist generated files & uploads across container restarts
      - ./uploads:/app/uploads
      - ./output:/app/output
    restart: unless-stopped
